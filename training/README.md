# Training pipeline (Phase 4)

Unsloth QLoRA + LLaMA-Factory jobs per blueprint Section 5.

- `configs/` — YAML hyperparameters per model
- Run on Colab T4 / Kaggle P100 / Modal H100
- Export via `scripts/export_model.py` (add when GPU training is scheduled)
