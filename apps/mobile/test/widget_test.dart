import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without errors', (WidgetTester tester) async {
    // Placeholder — integration tests are in tests/ at repo root.
    expect(1 + 1, equals(2));
  });
}
